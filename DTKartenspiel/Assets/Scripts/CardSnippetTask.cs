using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardSnippetTask : Card
{
    public CardSnippetTask(string id, Texture2D tex)
    {
        base.id = id;
        base.tex = tex;
    }
}
